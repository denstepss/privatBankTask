{% if categories|length > 0 %}
    <div>
        {% for category in categories %}
            <a href="{{ path('category', {'categoryId': category.id}) }}">{{ category.name }}</a>
        {% endfor %}
    </div>
{% endif %}